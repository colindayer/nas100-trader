---
type: journal
date: 2026-08-13
summary: CRITICAL: time exits were never implemented
tags: [journal]
---
# 2026-08-13

**Commits today (1):**

- CRITICAL: time exits were never implemented (`214c3fc`)

- CRITICAL: FTMO drawdown limits were inert (`d33b8a6`)
- Obsidian bridge auto-sync [bridge-auto] (`1ae9ede`)
- AutoTrading-off must not cost a bot its trading day; anchor on the real deposit (`addf922`)
- Obsidian bridge auto-sync [bridge-auto] (`5fc763f`)
- Anchor: MT5 needs naive datetimes, and the fallback must not be silent (`8fc0bf4`)
- Obsidian bridge auto-sync [bridge-auto] (`ce17bf8`)
- Market state: 60/120/250-day levels -- a 20-day window cannot see a 3-month peak (`f3cd185`)
- Obsidian bridge auto-sync [bridge-auto] (`7c29b87`)
- Regime must be positively identified -- 'mixed' meant 'detected nothing' (`382673a`)
- Obsidian bridge auto-sync [bridge-auto] (`b7773e5`)
- Desk as allocator: opportunity engine, specialists, playbooks, CIO, BOT_G (`4a2dbee`)
- Obsidian bridge auto-sync [bridge-auto] (`b747942`)
- CIO ranks by expected utility instead of freezing (`ff5c99f`)
- Obsidian bridge auto-sync [bridge-auto] (`139822f`)
- BOT_H liquidity sweep: closes the TRANSITION coverage gap (`1f43f61`)
- Obsidian bridge auto-sync [bridge-auto] (`e3b7e7e`)
- head_trader.py: the desk reviews itself, on the machine that trades (`cf10c77`)
- Obsidian bridge auto-sync [bridge-auto] (`5e944ed`)
- UTF-8 everywhere: the nightly review died on a Windows encoding error (`d7a9dca`)
- Obsidian bridge auto-sync [bridge-auto] (`d582e0a`)
- Reports written with a BOM so Windows tools read them correctly (`c6f4b9c`)
- Obsidian bridge auto-sync [bridge-auto] (`8bab06a`)
- CRITICAL: MT5 times are SERVER time -- every session window fired 3 hours early (`05f9b26`)
- Obsidian bridge auto-sync [bridge-auto] (`1a58a93`)
- Void the pre-clock-fix evidence without deleting any of it (`b46f2ec`)
- Obsidian bridge auto-sync [bridge-auto] (`7d80635`)
- Overnight mission: preflight, data-integrity gate, validity flag, evidence report (`b22d519`)
- Obsidian bridge auto-sync [bridge-auto] (`29f9607`)
- Preflight caught it: 15-minute rounding manufactured 6 minutes of clock drift (`c457cd0`)
- Obsidian bridge auto-sync [bridge-auto] (`6ea5603`)
- BOT_I: Asian sweep -> London reversal, as a SHADOW specialist (`5ffb344`)
- Obsidian bridge auto-sync [bridge-auto] (`d30bc67`)
- Raise spread floor 8x -> 30x: cost control is the lever a zero-edge system owns (`b387f29`)
- Obsidian bridge auto-sync [bridge-auto] (`ae3a8ec`)
- Fix test assertion: an 8x-spread stop is rejected, not widened (`9c1888c`)
- Obsidian bridge auto-sync [bridge-auto] (`2645536`)
- One clock for the desk: the broker's. Host drift no longer reaches a window (`7b57f4a`)
- Obsidian bridge auto-sync [bridge-auto] (`f91f9e4`)
_Back: [[11-Daily-Journal/_index|Daily Journal]]_
