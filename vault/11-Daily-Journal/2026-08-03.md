---
type: journal
date: 2026-08-03
summary: safety: bind equity baselines to an account; stop scoring broker rejections as critical
tags: [journal]
---
# 2026-08-03

**Commits today (1):**

- safety: bind equity baselines to an account; stop scoring broker rejections as critical (`17db5bd`)

- belief: annulment for evidence that misdescribes what happened + admin CLI (`5868860`)
- Obsidian bridge auto-sync [bridge-auto] (`7f697ea`)
- release 2026.08.03: ship MANIFEST.json with the sync so a deployment verifies itself (`32a9a9f`)
- Obsidian bridge auto-sync [bridge-auto] (`97cf8a4`)
- export OHLC from MT5 — the data the multisleeve strategy was never backtested on (`2a0b52c`)
- Obsidian bridge auto-sync [bridge-auto] (`762aeeb`)
- backtest: the first real equity curve for portfolio_multisleeve, via the production function (`c47ca84`)
- Obsidian bridge auto-sync [bridge-auto] (`6c3a543`)
- export_history: per-symbol progress + flush; --daily-only; lighter H1 default (`f275df2`)
- Obsidian bridge auto-sync [bridge-auto] (`bce1ff4`)
- backtest: universe tiers + a window-equivalence proof before any speed shortcut (`3dad140`)
- Obsidian bridge auto-sync [bridge-auto] (`8c0aa72`)
- frozen portfolio: autonomous rebalance loop, parity proven, execution still gated (`183c1a5`)
- Obsidian bridge auto-sync [bridge-auto] (`d87985a`)
- guardian: bind INITIAL_BALANCE to the connected account; fail closed on mismatch (`12d2a90`)
- Obsidian bridge auto-sync [bridge-auto] (`4dd6dee`)
- manifest: regenerate at 7829fb5 so a synced VPS reads COMPLETE (`48ef05f`)
- Obsidian bridge auto-sync [bridge-auto] (`7829fb5`)
- frozen portfolio: order submission, broker-side stop verification, fill audit (`53862f8`)
- Obsidian bridge auto-sync [bridge-auto] (`fce8a32`)
- preflight: check the terminal AlgoTrading switch; pre-trade rejections stay retryable (`85ed30c`)
- Obsidian bridge auto-sync [bridge-auto] (`c13ce29`)
- manifest: restamp at the shipping commit (`1f33b9b`)
- Obsidian bridge auto-sync [bridge-auto] (`1f68df2`)
- live fix: order comment must match the ledger, or we orphan our own position (`563a61f`)
- Obsidian bridge auto-sync [bridge-auto] (`ca841a4`)
- manifest: restamp at shipping commit (`2104a97`)
- Obsidian bridge auto-sync [bridge-auto] (`61327b1`)
- execution: three-concept account risk, tranching, partial fills, reconciliation proof (`60ecc79`)
- Obsidian bridge auto-sync [bridge-auto] (`672b7ad`)
- manifest: restamp at shipping commit (`64c89a0`)
- Obsidian bridge auto-sync [bridge-auto] (`4d58b18`)
- fix four defects the first live dry-run exposed (`69134bd`)
- Obsidian bridge auto-sync [bridge-auto] (`e5ad178`)
- manifest: restamp at shipping commit (`2d8ebbb`)
- Obsidian bridge auto-sync [bridge-auto] (`5dffe09`)
- preflight: require a single writer on the account (`264be66`)
- Obsidian bridge auto-sync [bridge-auto] (`7fa04c7`)
- manifest: restamp (`e1711cf`)
- Obsidian bridge auto-sync [bridge-auto] (`8b57874`)
- reconcile-proof: match the gate's bar instead of being stricter than it (`a96b1fe`)
- Obsidian bridge auto-sync [bridge-auto] (`d1d1ccd`)
- manifest: restamp (`576830a`)
- Obsidian bridge auto-sync [bridge-auto] (`9a43fb6`)
- execution modes: fail-closed only where money or duplicate exposure is at stake (`2df8566`)
- Obsidian bridge auto-sync [bridge-auto] (`b214145`)
- manifest: restamp (`084999d`)
- Obsidian bridge auto-sync [bridge-auto] (`f69c54f`)
- diagnostics: order_check before every send, full symbol metadata, named retcodes (`b26d7cb`)
- Obsidian bridge auto-sync [bridge-auto] (`5a20c84`)
- manifest: restamp (`5c1ebb2`)
- Obsidian bridge auto-sync [bridge-auto] (`e5b3e28`)
- diagnose: correct the tick-age calculation and dump the broker's declared sessions (`d0179ca`)
- Obsidian bridge auto-sync [bridge-auto] (`d326c42`)
- manifest: restamp (`50b41f3`)
- Obsidian bridge auto-sync [bridge-auto] (`6590495`)
- diagnose: remove a call to an API that does not exist; add --probe to settle 10018 (`c59ba01`)
- Obsidian bridge auto-sync [bridge-auto] (`e237b07`)
_Back: [[11-Daily-Journal/_index|Daily Journal]]_
